# Top-level dependency summary for the monitoring project
#
# This file aggregates the minimal, shared dependencies needed to run both
# the Windows Aggregator and Linux Agent. Per-component lockfiles still live in:
#   - ops/monitor/aggregator/requirements.txt
#   - ops/monitor/agent/requirements.txt
#
# Install (Aggregator, recommended venv):
#   python -m venv .venv
#   .\.venv\Scripts\pip install -r requirements.txt
#
# Install (Agent, recommended venv):
#   python3 -m venv venv
#   ./venv/bin/pip install -r requirements.txt
#
# Notes:
# - Uvicorn extras are included for production-ready asyncio/uvloop support.
# - GPU support relies on NVIDIA tooling (nvidia-smi); no extra PyPI package is
#   required unless you choose to install nvidia-ml-py in the agent env.

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
PyYAML>=6.0
psutil>=5.9.0         # agent metrics
python-json-logger>=2.0.0
apscheduler>=3.10.4   # aggregator scheduling
aiosqlite>=0.19.0     # aggregator SQLite access

# Dev/Test (optional)
pytest>=7.0.0
pytest-asyncio>=0.21.0
